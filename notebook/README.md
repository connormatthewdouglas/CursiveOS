# CursiveOS notebook

This is the organism website from the 2026-09 sprint: genome, machines, work,
ideas, join. It is the **public science log**. The Desktop CursiveOS window is
the task manager on a tester PC (`docs/os0-client-window.md`). They are one
product, two surfaces.

GitHub Pages still serves the older static page in `/dashboard` until this app
is built and swapped. Source of truth for the new UI is this folder.

Do not put occupancy ("this PC is busy") here.

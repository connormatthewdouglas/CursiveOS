# CursiveRoot

The public face of the organism: genome, machines, work, ideas, join.
This is the science log. **CursiveOS** on the Desktop is the task manager
on a tester PC (`docs/os0-client-window.md`). One product, two surfaces.

GitHub Pages still serves the older static page in `/dashboard` until this
app is built and swapped. Source for the new UI is this folder.

Do not put occupancy ("this PC is busy") here.
